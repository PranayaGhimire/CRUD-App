<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
</head>
<body class="bg-zinc-900">
    <div class="w-full h-full  p-10 text-white">
       
        <div class=" flex flex-wrap gap-8 nav mb-10">
            <a class="text-blue-500" href="/">Back to Home</a>
            <a class="text-blue-500" href="/read">Read Users</a>
        </div>
        <h1 class="text-2xl tracking-tighter mb-3">Update User</h1>
        <form action="/update/<%= user._id %>" method="post">
            <input class="px-5 py-2 border-2 border-zinc-800 outline-none rounded-lg bg-transparent" type="text" name="name" placeholder="name"  value="<%= user ? user.name : '' %>">
            <input class="px-5 py-2 border-2 border-zinc-800 outline-none rounded-lg bg-transparent" type="text" name="email" placeholder="email" value="<%= user ? user.email : '' %>">
            <input class="px-5 py-2 border-2 border-zinc-800 outline-none rounded-lg bg-transparent" type="text" name="image" placeholder="image url" value="<%= user ? user.image :'' %>">
            <input class="px-5 py-2 bg-green-500 rounded-lg p-5 ml-3 mt-3 md:mt-0 hover:bg-green-700 cursor-pointer" type="submit" value="Update User">

        </form>
        
    </div>
</body>
</html>